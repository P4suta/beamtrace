<!-- SPDX-License-Identifier: Apache-2.0 OR MIT -->
# Read-only v1 golden input

These canonical examples represent the final v1 manifest and event contract.
Tests package them into a v1 `.beamtrace` container, verify read compatibility,
and migrate them to a separate v2 archive. BeamTrace never writes v1 output.
